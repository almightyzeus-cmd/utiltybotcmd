const discord = require("discord.js");

module.exports = {
name: "shop"
usage: "Noth",
ownerOnly: false, 
cooldown: 5000,
botPermission: [],
authorPermission: [],
aliases: [],
description: "Nothing",
run: async (client, message, args) => {
  const shop = new discord.MessageEmbed()
  .setTitle('Shop:')
  
}
}